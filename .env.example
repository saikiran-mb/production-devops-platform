# Copy this file to .env and fill in real values.
# .env itself must NEVER be committed - it's already in .gitignore.

DB_PASSWORD=changeme_use_a_real_password_locally
SONARQUBE_DB_PASSWORD=changeme_use_a_different_real_password_locally
